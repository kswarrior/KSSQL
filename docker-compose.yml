version: '3.8'
services:
  ks-sql:
    build: .
    ports:
      - "5432:5432"
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - KSQL_ADMIN=admin
      - KSQL_PASSWORD=password
    restart: always
